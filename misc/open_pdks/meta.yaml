# Use `conda-build-prepare` before building for a better version string.
{% set version = '%s_%04i_%s'|format(GIT_DESCRIBE_TAG|replace('open_pdks-', '')|replace('-', '') or '0.X', GIT_DESCRIBE_NUMBER|int, GIT_DESCRIBE_HASH or 'gUNKNOWN') %}

package:
  name: open_pdks
  version: {{ version }}

source:
  - git_url: https://github.com/RTimothyEdwards/open_pdks.git
    git_rev: master
  - git_url: https://github.com/google/skywater-pdk.git
    git_rev: main    
    folder: skywater-pdk

build:
  noarch: generic
  # number: 202202031935
  number: {{ environ.get('DATE_NUM') }}
  # string: 20220203_1935
  string: {{ environ.get('DATE_STR') }}
  script_env:
    - CI
    
requirements:
  run:
    open_pdks.sky130a

outputs:
  - name: python-skywater-pdk
    version: 0.0.0
    build:
      noarch: python
      script: python -m pip install skywater-pdk/scripts/python-skywater-pdk/
    requirements:
      host:
        - dataclasses-json
        - python
        - pip
      run:
        - dataclasses-json
        - python
    test:
      imports: skywater_pdk
  - name: open_pdks.sky130a
    build:
      noarch: generic
    script: build-variant.sh
    requirements:
      build:
        - autoconf
        - automake
        - make
      host:
        - python
        - magic
        - python-skywater-pdk
    test:
      commands:
        - test -d $PREFIX/share/pdk/sky130A/libs.ref/sky130_fd_sc_hd
        - test -d $PREFIX/share/pdk/sky130A/libs.ref/sky130_fd_sc_hvl
        - test -d $PREFIX/share/pdk/sky130A/libs.ref/sky130_fd_io
        - test -d $PREFIX/share/pdk/sky130A/libs.ref/sky130_fd_pr
  
about:
  home: http://opencircuitdesign.com/open_pdks/
  license: Apache-2.0
  summary: 'PDK installer for open-source EDA tools and toolchains. Distributed with a setup for the Google/SkyWater 130nm process.'

extra:
  recipe-maintainers:
    - proppy
